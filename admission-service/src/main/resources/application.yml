eureka:
  client:
    service-url:
      default-zone:  http://localhost:8761/eureka/
    fetch-registry: true
  instance:
    prefer-ip-address: true

server:
  port: 1111
spring:
  application:
    name: Admission-Service







